apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: nightmare-project
data:
  app-name: "MEBN Task Manager"
  app-version: "1.0.0"
  backend-port: "5000"
  frontend-port: "80"
  api-base-url: "/api"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: database-config
  namespace: nightmare-project
data:
  database-type: "sqlite"
  database-file: "/data/tasks.db"
  database-init: |
    CREATE TABLE IF NOT EXISTS tasks (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      title TEXT NOT NULL,
      description TEXT,
      status TEXT DEFAULT 'pending',
      priority TEXT DEFAULT 'medium',
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    
    INSERT OR IGNORE INTO tasks (title, description, status, priority) VALUES
    ('Deploy Kubernetes', 'Complete homework assignment', 'completed', 'high'),
    ('Configure MEBN Stack', 'Python Flask + SQLite + Nginx', 'in progress', 'high'),
    ('Test Application', 'Full end-to-end testing', 'pending', 'medium');